# The bot's Discord token. NEVER commit this file with a real token.
# Copy it to ".env" and fill in your values.
DISCORD_TOKEN=your-bot-token-here

# The bot application's Client ID (Application ID from the Discord Developer Portal).
# Used by deploy-commands.js to register slash commands.
CLIENT_ID=your-bot-client-id-here
